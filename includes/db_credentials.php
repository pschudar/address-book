<?php
/*
 * Database Credentials
 * 
 * Keeping credentials in a separate file is beneficial for several reasons:
 * 1. It's easy to prevent this file from being read by a source code manager
 * 2. Allows unique credentials for development and production
 * 3. Allows multiple developers to work with their own unique credentials.
 */

const DB_SERVER = 'localhost';
const DB_USER = 'root';
const DB_PASS = '';
const DB_NAME = 'address_book';

